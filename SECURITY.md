# Security policy

Do not report vulnerabilities in a public issue. Contact the repository owner privately.

Never commit API keys, `.env` files, databases, memory stores, logs, or user data. Rotate any credential immediately if it is exposed.

This project is currently a development prototype. Do not expose it publicly until authentication, durable tenant isolation, rate limiting, migrations, audit logging, and a production secrets manager are implemented and reviewed.
